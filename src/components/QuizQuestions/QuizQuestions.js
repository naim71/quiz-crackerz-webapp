import React from 'react';

const QuizQuestions = ({qstn}) => {
    // console.log(qstn);
    const {id, qustion, correctAnswer, options} = qstn;
    return (
        <div>
            <h1>Quiz of {}</h1>
        </div>
    );
};

export default QuizQuestions;